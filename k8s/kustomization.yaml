apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# codex 認証はコンテナ内で行う(UIから device code フロー、~/.codex は PVC に永続化)。
# ホスト側からトークンを持ち込まない。
resources:
- pvc.yaml
- deployment.yaml
- service.yaml
images:
- name: ghcr.io/onethingtsukuba/startix-issue-box
  newName: ghcr.io/onethingtsukuba/startix-issue-box
  newTag: 1b6d10f5a420
